<!DOCTYPE html>
<html>
  <head>
    <title>XField</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <script nonce="<%= content_security_policy_nonce %>" src="https://cdn.tailwindcss.com"></script>
    <script nonce="<%= content_security_policy_nonce %>">
      tailwind.config = {
        darkMode: 'class',
        theme: {
          extend: {
            colors: {
              primary: {
                50: '#f5f8ff',
                100: '#e0e7ff',
                500: '#6366f1',
                600: '#4f46e5',
                700: '#4338ca'
              }
            },
            fontFamily: {
              display: ['"Space Grotesk"', 'sans-serif'],
              sans: ['"Inter"', 'sans-serif']
            }
          }
        }
      }
    </script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Space+Grotesk:wght@500;600;700&display=swap" rel="stylesheet">
  </head>
  <body class="min-h-screen bg-slate-950 bg-gradient-to-b from-slate-950 via-slate-950 to-slate-900 font-sans text-slate-100">
    <div class="relative">
      <div class="pointer-events-none absolute inset-0 overflow-hidden">
        <div class="absolute -left-20 top-16 h-64 w-64 rounded-full bg-primary-600/40 blur-3xl"></div>
        <div class="absolute right-[-6rem] top-72 h-72 w-72 rounded-full bg-blue-500/30 blur-3xl"></div>
      </div>
      <%= render "shared/navbar" %>
      <% if notice.present? %>
        <div class="mx-auto mt-6 max-w-3xl rounded-xl border border-slate-700/60 bg-slate-900/80 px-4 py-3 text-sm text-green-300 backdrop-blur">
          <%= notice %>
        </div>
      <% end %>
      <% if alert.present? %>
        <div class="mx-auto mt-6 max-w-3xl rounded-xl border border-slate-700/60 bg-slate-900/80 px-4 py-3 text-sm text-rose-300 backdrop-blur">
          <%= alert %>
        </div>
      <% end %>
      <main class="relative z-10">
        <%= yield %>
      </main>
      <%= render "shared/footer" %>
    </div>
  </body>
</html>
