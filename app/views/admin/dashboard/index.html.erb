<section>
  <h1 class="font-display text-3xl font-semibold text-white">Welcome back</h1>
  <p class="mt-2 text-sm text-slate-400">Monitor the health of XField and approve new submissions.</p>
  <div class="mt-8 grid gap-6 md:grid-cols-2 xl:grid-cols-4">
    <div class="rounded-2xl border border-slate-800 bg-slate-900/80 p-5">
      <p class="text-xs uppercase tracking-[0.3em] text-slate-500">Articles</p>
      <p class="mt-3 text-3xl font-semibold text-white"><%= @articles_count %></p>
    </div>
    <div class="rounded-2xl border border-slate-800 bg-slate-900/80 p-5">
      <p class="text-xs uppercase tracking-[0.3em] text-slate-500">Opportunities pending</p>
      <p class="mt-3 text-3xl font-semibold text-white"><%= @opportunities_pending %></p>
    </div>
    <div class="rounded-2xl border border-slate-800 bg-slate-900/80 p-5">
      <p class="text-xs uppercase tracking-[0.3em] text-slate-500">Investors pending</p>
      <p class="mt-3 text-3xl font-semibold text-white"><%= @investors_pending %></p>
    </div>
    <div class="rounded-2xl border border-slate-800 bg-slate-900/80 p-5">
      <p class="text-xs uppercase tracking-[0.3em] text-slate-500">Subscribers</p>
      <p class="mt-3 text-3xl font-semibold text-white"><%= @users_count %></p>
    </div>
  </div>
</section>
